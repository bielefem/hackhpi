<%= bootstrap_form_for(patient) do |form| %>
  <% if patient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(patient.errors.count, "error") %> prohibited this patient from being saved:</h2>

      <ul>
      <% patient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :firstname, id: :patient_firstname %>
  </div>

  <div class="field">
    <%= form.text_field :lastname, id: :patient_lastname %>
  </div>

  <div class="field">
    <%= form.date_select :birthday, id: :patient_birthday %>
  </div>

  <div class="field">
    <%= form.text_field :email, id: :patient_email %>
  </div>

  <div class="field">
    <%= form.text_area :publickey, id: :patient_publickey %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
